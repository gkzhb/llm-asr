# R5 independent review disposition — pending repair

Source review `reports/review/phase20-r5-independent.md` complete; no device/test execution by reviewer. Parent baseline green remains earlier source evidence only.

| Finding | Decision | Required verification |
|---|---|---|
| IR-1 QUEUED status may remain while provider blocks in WRITING | Accept. Lifecycle-scoped coalesced foreground refresh every ~250ms while busy; remove onPause and stop when terminal. Do not call arbitrary listener between write admission and open. Also make queued wording describe conditional revocation without guaranteeing current write is revocable. | A scheduler/page fixture or explicit source+negative guards proves only one scheduled render, pause removes, terminal stops. Actual Android Handler/SAF remains device pending. |
| IR-2 ordinary write failure + fatal close swallowed | Accept. Explicit write/close arbitration or suppressed fatal propagation, preserving primary fatal identity, close attempt once, slot until close return; fixed public status. | Production fake-stream exact identity tests for IOException+InternalError close and primary Error+IOException close, plus blocked close. |
| Shared owner/AppState tests disconnected | Accept evidence gap. Replace dummy-state assertions with single production AppState.resultState, real RequestRunner/AppRequestPolicy and coordinator fixture. Android AsrOperation still compile/source-only, do not call fixture full Graph integration. | Run real runner operations with shared state during blocked export, assert current text/status/report side effects and actual clear epoch. Separate Graph/Asr source guards with negative fixtures. |
| Deferred/duplicate/begin race/listener cases absent | Add targeted tests using actual page/controller, bounded latch schedule both before/after admission, assertions outside observer. | deferred clear/destroy; repeated callback/resume; old same-page callback; throwing sibling listener; removal and rejection/fatal notifications. |
| Exact cap/privacy edge | Add 100000 UTF16 units accepted UTF8 data, 100001 no opens; distinct sensitive sentinels excluded from status. | deterministic production tests. |
| Clear tap != worker-body clear epoch | Document. Preserve original owner/preflight semantics; actual revocation occurs in accepted clear transaction, not at button click. | source contract and user text should not promise immediate click-time cancellation. |

Fixes applied by parent and full host/SDK35 javac passed. Logs `.work/refactor-phase20-r6/policy-final-{host,javac}.log`; compound fatal and model notification have explicit compile-success/AssertionError red then green evidence. Additional tests now use same AppState.resultState plus actual RequestRunner/AppRequestPolicy during blocked export, deferred clear/destroy/duplicate, exact supplementary-character cap, throwing observer. These remain pure-Java production-port tests, not Android AppGraph/AsrOperation runtime. Foreground busy rendering has source guards/8 negative fixtures, not a real Handler scheduling test. Missing real Android scheduling and exhaustive begin/clear race coverage remain disclosed. Final independent narrow review and full APK pending; R6 package migration now underway.
